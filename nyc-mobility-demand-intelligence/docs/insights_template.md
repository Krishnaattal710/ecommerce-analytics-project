# Insights Template - NYC Mobility Project

1. Peak demand window:
- Highest hourly trip volume occurs around `HH:00` with `X` average trips.

2. Borough concentration:
- `Borough A` contributes `Y%` of trip volume and should be prioritized for fleet allocation.

3. Revenue quality:
- Average fare is `F`, with payment mix indicating `credit/cash` preference.

4. Route intelligence:
- Top route (`Zone A` -> `Zone B`) contributes `R` trips, indicating strong recurring demand.

5. Planning signal:
- 7-day baseline forecast suggests demand of `N` trips/day, supporting staffing planning.

## Recommendations

- Shift supply to top demand hours and zones.
- Focus operations and routing around top O-D corridors.
- Use forecast output for daily dispatch planning.
